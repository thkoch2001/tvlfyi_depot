build:
  roots:
    #- proto
    - third_party
lint:
  ignore:
    - nix/buildGo
  use:
    - BASIC
    - FILE_LOWER_SNAKE_CASE
  except:
    - ENUM_VALUE_UPPER_SNAKE_CASE
    - PACKAGE_DIRECTORY_MATCH
